/*
 * Esharq, a Discord client mod
 * Copyright (c) 2026 LOSTSTR
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import "./surfaces.css";

/**
 * رموز تصميم إشراق — الألوان والمقاسات والترتيب.
 *
 * مصدر الحقيقة الوحيد لهوية إشراق البصرية: كل مكوّن يقرأ من هنا، فالتغيير
 * في مكان واحد. القيم مختارة لتوحيد الإحساس عبر الأقسام الخمسة.
 *
 * 🔴 **والاستيراد أعلاه ليس زينة**: `SURFACE` أدناه صارت إشاراتٍ إلى متغيّرات
 * CSS، ومن يقرأها بلا تحميل `surfaces.css` يحصل على `var()` بلا قيمة. ووضعُه
 * هنا يضمن أنّ كلّ من استورد الأسطح استورد تعريفها معها.
 */

/** لون التمييز — ذهب إشراق، يميّزه عن أزرق ديسكورد. للتعبئة والحدود. */
export const ACCENT = "#c9a227";

/**
 * الذهب **نصّاً**.
 *
 * 🔴 لا يصلح `ACCENT` لوناً لنصٍّ في الثيم الفاتح: تباين `#c9a227` على أبيض
 * ٢٫١ — تحت الحدّ الأدنى ٤٫٥. فالنصّ الذهبيّ يغمق مع الثيم، والتعبئة لا.
 */
export const ACCENT_TEXT = "var(--esharq-on-accent)";
export const ACCENT_SOFT = "#c9a22722";
export const ACCENT_STRONG = "#a8851d";

/** وحدة المباعدة الأساسية. كل الهوامش مضاعفاتها. */
export const UNIT = 7;

/** نصف قطر الحوافّ في البطاقات والأزرار. */
export const RADIUS = 11;

/**
 * ترتيب الأقسام الخمسة. الرقم يحدّد الموضع في الشريط، والفجوات مقصودة
 * لإتاحة الإدراج بين الأقسام لاحقاً بلا إعادة ترقيم.
 */
export const SECTION_ORDER: Record<string, number> = {
    "essentials": 20,
    "safety-health": 42,
    "updates-community": 64,
    "tools": 86,
    "data-support": 108
};

/** مدّة الانتقالات — موحّدة عبر الواجهة. */
export const TRANSITION_MS = 140;

/**
 * درجات الأسطح: ٠ بئرٌ غائرة · ١ بطاقة · ٢ لوحةٌ داخلها · ٣ شارةٌ أو زرّ.
 *
 * 🔴 **متغيّراتٌ لا أرقامٌ ستّ عشريّة**: كانت مثبَّتةً سوداء فبقيت سوداء حين
 * يختار المستخدم الثيم الفاتح، بينما ينقلب النصّ الموروث إلى داكن — فتباينٌ
 * قِيس **١٫٣٢** على كناري، أي نصٌّ غير مرئيّ في أربعٍ وعشرين صفحة. والقيم
 * نفسها لم تتغيّر في الثيم الداكن: انظر `surfaces.css`.
 */
export const SURFACE = [
    "var(--esharq-surface-0)",
    "var(--esharq-surface-1)",
    "var(--esharq-surface-2)",
    "var(--esharq-surface-3)"
] as const;

dist
node_modules
.claude
installer/*.exe
installer/*.ico
# الملف المُولَّد تلقائياً من القالب أثناء البناء — لا يُتتبَّع
installer/EquicordArabic-Installer.ps1
.VSCodeCounter
.vars
.secrets
.input

# MCP server config — may contain tokens or sensitive tool config
.mcp.json

# Environment & secrets — never commit these
.env
.env.*
.env.local
.env.production
.env.development
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
*.secret

*.exe
vencord_installer
equicord_installer

.idea
.DS_Store

yarn.lock
bun.lock
package-lock.json

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
*.tsbuildinfo

.modules/
# src/userplugins is included intentionally for Esharq

ExtensionCache/
/settings

# vencord companion module cache
.modules

# wakatime related stuff
.wakatime-project

*.tmp
_claude_memory/
.work/

# أداة كشف المنشأ — خاصة، لا تُرفَع
scripts/esharq/.provenance-scan.mjs

# مخرجات التغطية القابلة لإعادة التوليد. اللقطة `discord-keys.json` وحدها
# تُلتزَم لأنها **مرجع المحرس**: بدونها لا يعرف أحد ما الذي أضافه ديسكورد.
src/userplugins/DiscordArabicizer/coverage/untranslated.json
src/userplugins/DiscordArabicizer/coverage/placeholder-mismatch.json
